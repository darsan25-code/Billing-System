/**
 * BillTable.js – Bill line-items table component (placeholder)
 *
 * Will render the table of products added to a bill.
 * Not implemented yet.
 */

const BillTable = (() => {
  function mount(container) {
    container.innerHTML = `<div class="page-placeholder">[ BillTable – not yet implemented ]</div>`;
  }

  return { mount };
})();
